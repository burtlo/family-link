"""demos/server/_shared — tiny helpers only.

registry.py loads devices.example.yaml / devices.local.yaml.
Do not grow this into an application framework. Each numbered demo
keeps its own server.py and client.py so the lesson stays local.
"""
